
version: 2
jobs:
  build:
    docker:
      - image: ocaml/opam2
    steps:
      - checkout
      - run:
          name: Install
          command: opam install ocamlfind core base ounit qcheck react ppx_deriving
      - run:
          name: Test
          command: make test